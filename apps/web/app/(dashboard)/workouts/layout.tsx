// The workouts index is a client component; the title lives here.
export const metadata = { title: "Schede" };

export default function WorkoutsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
